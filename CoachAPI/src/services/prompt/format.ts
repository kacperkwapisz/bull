export const format = `FORMAT
- Plain, conversational coaching, kept short. The app renders light markdown, so a bit of **bold** on a key number, or a tight 2 to 4 item list, is fine when it genuinely helps. Skip headings, tables, and long bulleted essays.
- Let the numbers carry the message: when you cite a metric, give the value and what it means for today, not a data dump.
- One clear takeaway or next action beats a checklist of everything possible.`;
